apiVersion: v1
kind: Service
metadata:
  name: data-demo-api
  namespace: staging
spec:
  selector:
    app: data-demo-api
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP
